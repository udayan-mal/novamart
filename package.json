{
  "name": "novamart",
  "version": "1.0.0",
  "private": true,
  "description": "NovaMart — Modern Multi-Vendor E-Commerce SaaS Platform",
  "keywords": ["ecommerce", "saas", "multi-vendor", "microservices", "marketplace", "react", "nodejs"],
  "author": "NovaMart Team",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,green \"npm run dev:client\" \"npm run dev:server\"",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "build": "cd client && npm run build",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "clean": "rimraf node_modules client/node_modules server/node_modules"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "rimraf": "^6.0.1"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
