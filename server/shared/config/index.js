const categories = require("./categories");
const theme = require("./theme");
const { SERVICE_PORTS, SERVICE_ROUTES } = require("./services");

module.exports = { categories, theme, SERVICE_PORTS, SERVICE_ROUTES };
